<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@android:color/transparent"/> <!-- Прозрачный фон -->
    <stroke android:width="2dp" android:color="@android:color/black"/> <!-- Цвет обводки -->
    <corners android:radius="12dp"/> <!-- Скруглённые углы -->
    <padding android:left="8dp" android:top="4dp" android:right="8dp" android:bottom="4dp"/>
</shape>
